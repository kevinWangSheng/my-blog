---
import ListPage from '../../components/ListPage.astro';
import { getPublicCollection } from '../../lib/content';
const entries = await getPublicCollection('projects');
---
<ListPage collection="projects" entries={entries} />
